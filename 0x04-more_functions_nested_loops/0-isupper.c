#include <stdio.h>
#include "main.h"
/**
 * _isupper - checks for uppercase character
 *
 * Return: 1 if true, 0 if otherwise
 */
int _isupper(int c)
{
	int c;
	int j;

	for (c = 'A'; c <= 'Z'; a++)
	{
		if (j = c)
			return (1);
		else
			return (0);
	}
}
