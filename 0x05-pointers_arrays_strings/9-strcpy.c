#include "main.h"
#include <stdio.h>
/**
 * _strcpy - opies the string pointed to by src, including the terminating null
 * byte (\0), to the buffer pointed to by dest
 * Return: pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	_strcpy (dest, src);
	printf("_strcpy( dest, src) :  %s\n", dest );

	return (dest);
}
